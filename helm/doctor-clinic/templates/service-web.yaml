apiVersion: v1
kind: Service
metadata:
  name: doctor-clinic-web
  namespace: {{ .Values.namespace }}
spec:
  selector:
    app: doctor-clinic-web
  ports:
    - port: {{ .Values.service.web.port }}
      targetPort: 80
      nodePort: 30080
  type: NodePort